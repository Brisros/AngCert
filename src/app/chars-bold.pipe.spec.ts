import { CharsBoldPipe } from './chars-bold.pipe';

describe('CharsBoldPipe', () => {
  it('create an instance', () => {
    const pipe = new CharsBoldPipe();
    expect(pipe).toBeTruthy();
  });
});
